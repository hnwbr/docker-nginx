server {

        listen 80;
        listen [::]:80;

        root /var/www/html;
        index index.html index.htm index.nginx-debian.html;

        server_name khoa-icu.hoisuc.net web.khoa.icu;

        location / {
                try_files $uri $uri/ =404;
        }

}

server {

        server_name pg.khoa-icu.hoisuc.net pg.khoa.icu;

        location / {
                proxy_pass http://localhost:5050;
        }

        listen 80;
        listen [::]:80;

}

server {

        server_name d.khoa.icu khoa.icu;

        location / {
                proxy_pass http://localhost:8055;
        }

        listen 80;
        listen [::]:80;

}

# server {

#         if ($host ~ ^[^.]+\.khoa\.icu$) {
#                 return 301 https://$host$request_uri;
#         } # managed by Certbot

#         if ($host = khoa.icu) {
#                 return 301 https://$host$request_uri;
#         } # managed by Certbot

#         listen 80 ;
#         listen [::]:80 ;
#         server_name khoa.icu;
#         return 404; # managed by Certbot

# }